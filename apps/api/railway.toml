[build]
buildCommand = "bun install && bun run build"

[deploy]
startCommand = "node ./build/src/main.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
