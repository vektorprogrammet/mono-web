import { createElement } from "react";
import { QUESTIONNAIRES_ELEMENT } from "../foldkit/recruitment-maintenance/elements";

export default function Intervjusjema() {
  return createElement(QUESTIONNAIRES_ELEMENT);
}
