import { createElement } from "react";
import { SCHOOL_SURVEYS_ELEMENT } from "../foldkit/surveys/elements";

// biome-ignore lint/style/noDefaultExport: Route Modules require default export
export default function Undersokelser() {
  return createElement(SCHOOL_SURVEYS_ELEMENT);
}
