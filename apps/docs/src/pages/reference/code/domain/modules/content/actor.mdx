---
title: Domain / content / actor
description: Generated code reference for packages/domain/src/content/actor.ts.
showAskAi: false
---

# Domain / `content / actor`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/content/actor.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [ContentActor (type alias)](#contentactor-type-alias)
  - [ContentActorDenial (type alias)](#contentactordenial-type-alias)
  - [canPublishContent](#canpublishcontent)
  - [canReviseDraft](#canrevisedraft)
  - [contentScopeFor](#contentscopefor)
  - [resolveContentActor](#resolvecontentactor)

---

## utils

### ContentActor (type alias)

The content actor derived from one Organization projection at one instant.

**Signature**

```ts
type ContentActor =
  | { readonly _tag: "ContentAdministrator"; readonly personId: PersonId }
  | {
      readonly _tag: "ContentPublisher";
      readonly personId: PersonId;
      readonly departmentIds: ReadonlyArray<DepartmentId>;
    }
  | {
      readonly _tag: "ContentEditor";
      readonly personId: PersonId;
      readonly departmentIds: ReadonlyArray<DepartmentId>;
    };
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L9)

### ContentActorDenial (type alias)

**Signature**

```ts
type ContentActorDenial = "AuthorityInactive" | "NotInScope";
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L22)

### canPublishContent

Whether the actor may set sticky or run publish/unpublish transitions.

**Signature**

```ts
declare const canPublishContent: (
  actor: ContentActor,
  articleDepartmentIds: ReadonlyArray<DepartmentId>,
) => boolean;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L91)

### canReviseDraft

Draft ownership boundary (spec 0062): editors revise only their own drafts
carrying at least one department within their active memberships; leaders
revise any draft scoped to their departments; administrators span all.

**Signature**

```ts
declare const canReviseDraft: (
  actor: ContentActor,
  draft: {
    readonly createdByPersonId: PersonId;
    readonly currentVersionNumber: number | null;
    readonly departmentIds: ReadonlyArray<DepartmentId>;
  },
) => boolean;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L107)

### contentScopeFor

The department intersection a publisher may see and revise.

**Signature**

```ts
declare const contentScopeFor: (
  actor: ContentActor,
) =>
  | { readonly _tag: "All" }
  | { readonly _tag: "DepartmentIds"; readonly departmentIds: ReadonlyArray<DepartmentId> };
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L75)

### resolveContentActor

Maps the complete Organization projection onto the content actor (spec 0062
§Actor model). Pure over its input; leadership never widens scope and an
administrator grant never combines with membership scoping.

**Signature**

```ts
declare const resolveContentActor: (
  authority: OrganizationPersonAuthority,
) => Decision<ContentActor>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/actor.ts#L32)
