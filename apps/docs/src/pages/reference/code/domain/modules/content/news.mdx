---
title: Domain / content / news
description: Generated code reference for packages/domain/src/content/news.ts.
showAskAi: false
---

# Domain / `content / news`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/content/news.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/news.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [readNewsListingPostgres](#readnewslistingpostgres)
  - [readPublishedArticlePostgres](#readpublishedarticlepostgres)

---

## utils

### readNewsListingPostgres

Reads every article's current published version in one repeatable-read,
write-free snapshot (laws 5, 7, 8). The inner join on the current-version
pointer admits only published state; drafts never surface.

**Signature**

```ts
declare const readNewsListingPostgres: (
  departmentId?: DepartmentId,
) => Effect.Effect<
  PublishedNewsListing,
  ContentDecodeError | ContentDepartmentNotFound | ContentIntegrityError,
  Database | Organization | Profile
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/news.ts#L58)

### readPublishedArticlePostgres

Resolves the current or one immutable historical version while the article
remains published. Unknown, withdrawn, and unknown-version reads share the
same typed not-found result.

**Signature**

```ts
declare const readPublishedArticlePostgres: (
  slug: string,
  versionNumber?: number,
) => Effect.Effect<
  PublishedNewsArticle,
  ContentDecodeError | ContentIntegrityError | ArticleNotFound,
  Database | Profile
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/news.ts#L151)
