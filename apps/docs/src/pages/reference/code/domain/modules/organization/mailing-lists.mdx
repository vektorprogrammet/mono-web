---
title: Domain / organization / mailing-lists
description: Generated code reference for packages/domain/src/organization/mailing-lists.ts.
showAskAi: false
---

# Domain / `organization / mailing-lists`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/organization/mailing-lists.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [MailingList (interface)](#mailinglist-interface)
  - [MailingListContact (interface)](#mailinglistcontact-interface)
  - [MailingListType (type alias)](#mailinglisttype-type-alias)
  - [MailingListsProjectInput (interface)](#mailinglistsprojectinput-interface)
  - [membershipCoversSemester](#membershipcoverssemester)
  - [projectOrganizationMailingLists](#projectorganizationmailinglists)

---

## utils

### MailingList (interface)

**Signature**

```ts
export interface MailingList {
  readonly name: string;
  readonly emails: ReadonlyArray<string>;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L30)

### MailingListContact (interface)

Profile contact read result keyed by person identity.

**Signature**

```ts
export interface MailingListContact {
  readonly name: string;
  readonly email: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L13)

### MailingListType (type alias)

Spec 0060: the native mailing-list projection. Pure derivation over injected
member rows and Profile contacts — zero persistence, no ambient lookups,
one authorizationInstant already applied by whoever produced the inputs.

**Signature**

```ts
type MailingListType = "assistants" | "team" | "all";
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L10)

### MailingListsProjectInput (interface)

**Signature**

```ts
export interface MailingListsProjectInput {
  readonly type: MailingListType;
  readonly authorizedDepartmentIds: ReadonlyArray<DepartmentId>;
  readonly departmentId?: DepartmentId;
  readonly semesterId?: SemesterId;
  /** Team members active across the requested semester, grouped by department. */
  readonly membersByDepartment: ReadonlyMap<DepartmentId, ReadonlyArray<PersonId>>;
  /** Assistant-history facts grouped by department (Admissions-owned source seam). */
  readonly assistantsByDepartment: ReadonlyMap<DepartmentId, ReadonlyArray<PersonId>>;
  readonly contacts: ReadonlyMap<PersonId, MailingListContact>;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L18)

### membershipCoversSemester

A half-open membership interval covers a semester when it started at or
before the semester's end and has not ended before the semester's start.

**Signature**

```ts
declare const membershipCoversSemester: (
  membership: { readonly startAt: string; readonly endAt: string | null },
  semester: { readonly startAt: string; readonly endAt: string },
) => boolean;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L39)

### projectOrganizationMailingLists

**Signature**

```ts
declare const projectOrganizationMailingLists: (
  input: MailingListsProjectInput,
) => ReadonlyArray<MailingList>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/organization/mailing-lists.ts#L61)
