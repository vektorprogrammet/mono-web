---
title: Domain / receipt / service
description: Generated code reference for packages/domain/src/receipt/service.ts.
showAskAi: false
---

# Domain / `receipt / service`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/receipt/service.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/service.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Portable Economy service contract for receipt lifecycle operations.

Since v0.1.0

---

### Exports Grouped by Category

- [utils](#utils)
  - [Economy (class)](#economy-class)
  - [EconomyShape (interface)](#economyshape-interface)
  - [ReceiptTransactionResult (interface)](#receipttransactionresult-interface)

---

## utils

### Economy (class)

**Signature**

```ts
declare class Economy
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/service.ts#L79)

### EconomyShape (interface)

**Signature**

```ts
export interface EconomyShape {
  readonly executeReceipt: (
    input: unknown,
    principal: ReceiptCommandPrincipal,
    allocation?: ReceiptSubmissionAllocation,
  ) => Effect.Effect<ReceiptTransactionResult, ReceiptFailure>;
  readonly listOwnedReceipts: (
    ownerPersonId: string,
    status?: ReceiptStatus,
  ) => Effect.Effect<ReadonlyArray<OwnedReceiptProjectionItem>, ReceiptPersistenceError>;
  readonly listReceiptsForApproval: (
    personId: PersonId,
    authorizationInstant: OrganizationAuthorityInstant,
    status?: ReceiptStatus,
  ) => Effect.Effect<ReadonlyArray<ReceiptListItem>, ReceiptApprovalListFailure>;
  readonly readReceiptLifecycleEvidence: (
    receiptId: string,
    ownerPersonId: string,
  ) => Effect.Effect<ReceiptLifecycleEvidenceProjection, ReceiptPersistenceError | ReceiptNotFound>;
  readonly receiptStatusTotals: Effect.Effect<
    ReadonlyArray<ReceiptStatusTotal>,
    ReceiptPersistenceError
  >;
  readonly listStaleOutboxClaims: (
    claimedBefore: string,
    receiptId?: string,
  ) => Effect.Effect<ReadonlyArray<string>, ReceiptPersistenceError>;
  readonly recoverStaleOutboxClaim: (
    claimId: string,
    claimedBefore: string,
  ) => Effect.Effect<number, ReceiptPersistenceError>;
  readonly deliverNextOutboxEffect: (
    claimId: string,
    claimedAt: string,
    receiptId?: string,
  ) => Effect.Effect<
    ReceiptOutboxDeliveryResult,
    ReceiptPersistenceError,
    ReceiptFileService | ReceiptAuxiliaryEffects
  >;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/service.ts#L37)

### ReceiptTransactionResult (interface)

**Signature**

```ts
export interface ReceiptTransactionResult {
  readonly observation: ReceiptObservation;
  readonly replayed: boolean;
  readonly outboxCount: number;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/service.ts#L31)
