---
title: Domain / receipt / update
description: Generated code reference for packages/domain/src/receipt/update.ts.
showAskAi: false
---

# Domain / `receipt / update`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/receipt/update.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/update.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [ReceiptDecision (interface)](#receiptdecision-interface)
  - [ReceiptDecisionContext (interface)](#receiptdecisioncontext-interface)
  - [ReceiptOutboxRequest](#receiptoutboxrequest)
  - [decideReceipt](#decidereceipt)

---

## utils

### ReceiptDecision (interface)

**Signature**

```ts
export interface ReceiptDecision {
  readonly receipt: Receipt;
  readonly observation: ReceiptObservation;
  readonly outbox: ReadonlyArray<ReceiptOutboxRequest>;
  readonly auditAction: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/update.ts#L61)

### ReceiptDecisionContext (interface)

**Signature**

```ts
export interface ReceiptDecisionContext {
  readonly receiptId: string;
  readonly visualId: string;
  readonly now: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/update.ts#L53)

### ReceiptOutboxRequest

**Signature**

```ts
declare const ReceiptOutboxRequest:
  | {
      readonly _tag: "PromoteReceiptFile";
      readonly file: {
        readonly byteLength: number;
        readonly fileRef: string;
        readonly objectKey: string;
        readonly contentType: "image/jpeg" | "image/png" | "application/pdf";
        readonly sha256: string;
      };
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    }
  | {
      readonly _tag: "DeleteReceiptFile";
      readonly file: {
        readonly byteLength: number;
        readonly fileRef: string;
        readonly objectKey: string;
        readonly contentType: "image/jpeg" | "image/png" | "application/pdf";
        readonly sha256: string;
      };
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    }
  | {
      readonly _tag: "NotifyEconomyReceiptSubmitted";
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    }
  | {
      readonly _tag: "NotifyReceiptRefunded";
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    }
  | {
      readonly _tag: "NotifyReceiptRejected";
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    }
  | {
      readonly _tag: "WriteReceiptAudit";
      readonly effectId: string;
      readonly receiptId: string;
      readonly commandId: string;
    };
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/update.ts#L59)

### decideReceipt

**Signature**

```ts
declare const decideReceipt: (
  existing: Receipt | undefined,
  input: unknown,
  context: ReceiptDecisionContext,
) => Effect.Effect<ReceiptDecision, ReceiptFailure>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/receipt/update.ts#L312)
