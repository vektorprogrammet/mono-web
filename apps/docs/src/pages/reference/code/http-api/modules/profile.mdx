---
title: HTTP API / profile
description: Generated code reference for packages/http-api/src/profile.ts.
showAskAi: false
---

# HTTP API / `profile`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/http-api/src/profile.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts).
> Package: `@vektorprogrammet/http-api@0.2.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Public HTTP contracts for the current user's profile.

Since v0.1.0

---

### Exports Grouped by Category

- [Endpoints](#endpoints)
  - [ReadOwnProfileEndpoint](#readownprofileendpoint)
  - [UpdateOwnProfileEndpoint](#updateownprofileendpoint)
- [Groups](#groups)
  - [ProfileApi (class)](#profileapi-class)
- [Schemas](#schemas)
  - [UserProfileResponse](#userprofileresponse)
  - [UserRoleSchema](#userroleschema)

---

## Endpoints

### ReadOwnProfileEndpoint

Reads the current person's profile.

**Signature**

```ts
declare const ReadOwnProfileEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "readOwnProfile",
  "GET",
  "/api/profile",
  never,
  never,
  never,
  Schema.toCodecStringTree<
    Schema.Struct<{
      readonly "if-match": Schema.optional<Schema.String>;
      readonly "if-none-match": Schema.optional<Schema.String>;
    }>
  >,
  | HttpApiSchema.WithHeaders<
      Schema.toCodecJson<
        Schema.Struct<{
          readonly personId: Schema.brand<Schema.String, "PersonId">;
          readonly firstName: Schema.String;
          readonly lastName: Schema.String;
          readonly email: Schema.String;
          readonly phone: Schema.String;
          readonly role: Schema.Literals<
            readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
          >;
          readonly nameRevision: Schema.Int;
          readonly contactRevision: Schema.Int;
        }>
      >,
      Schema.toCodecStringTree<
        Schema.Struct<{
          ETag: Schema.brand<Schema.String, "StrongETag">;
          "Cache-Control": Schema.Literal<"private, no-store">;
          Vary: Schema.Literal<"Origin">;
        }>
      >
    >
  | HttpApiSchema.WithHeaders<
      Schema.toCodecJson<Schema.Void>,
      Schema.toCodecStringTree<
        Schema.Struct<{
          ETag: Schema.brand<Schema.String, "StrongETag">;
          "Cache-Control": Schema.Literal<"private, no-store">;
          Vary: Schema.Literal<"Origin">;
        }>
      >
    >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  PersonSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L75)

Since v0.1.0

### UpdateOwnProfileEndpoint

Updates the current person's profile with optimistic revisions.

**Signature**

```ts
declare const UpdateOwnProfileEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "updateOwnProfile",
  "PATCH",
  "/api/profile",
  never,
  never,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly firstName: Schema.optional<Schema.NullOr<Schema.String>>;
      readonly lastName: Schema.optional<Schema.NullOr<Schema.String>>;
      readonly email: Schema.optional<Schema.NullOr<Schema.String>>;
      readonly phone: Schema.optional<Schema.NullOr<Schema.String>>;
    }>
  >,
  Schema.toCodecStringTree<
    Schema.Struct<{
      readonly "idempotency-key": Schema.brand<Schema.String, "IdempotencyKey">;
      readonly "if-match": Schema.brand<Schema.String, "StrongETag">;
    }>
  >,
  HttpApiSchema.WithHeaders<
    Schema.toCodecJson<
      Schema.Struct<{
        readonly personId: Schema.brand<Schema.String, "PersonId">;
        readonly firstName: Schema.String;
        readonly lastName: Schema.String;
        readonly email: Schema.String;
        readonly phone: Schema.String;
        readonly role: Schema.Literals<
          readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
        >;
        readonly nameRevision: Schema.Int;
        readonly contactRevision: Schema.Int;
      }>
    >,
    Schema.toCodecStringTree<
      Schema.Struct<{
        ETag: Schema.brand<Schema.String, "StrongETag">;
        "Cache-Control": Schema.Literal<"no-store">;
        Vary: Schema.Literal<"Origin">;
      }>
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  PersonSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L105)

Since v0.1.0

## Groups

### ProfileApi (class)

Current-person profile endpoints.

**Signature**

```ts
declare class ProfileApi
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L138)

Since v0.1.0

## Schemas

### UserProfileResponse

Strict self-profile response exposed to the dashboard.

**Signature**

```ts
declare const UserProfileResponse: Schema.Struct<{
  readonly personId: Schema.brand<Schema.String, "PersonId">;
  readonly firstName: Schema.String;
  readonly lastName: Schema.String;
  readonly email: Schema.String;
  readonly phone: Schema.String;
  readonly role: Schema.Literals<readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]>;
  readonly nameRevision: Schema.Int;
  readonly contactRevision: Schema.Int;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L43)

Since v0.1.0

### UserRoleSchema

Legacy-compatible dashboard role projection.

**Signature**

```ts
declare const UserRoleSchema: Schema.Literals<
  readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L31)

Since v0.1.0
