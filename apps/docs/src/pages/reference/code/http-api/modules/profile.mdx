---
title: HTTP API / profile
description: Generated code reference for packages/http-api/src/profile.ts.
showAskAi: false
---

# HTTP API / `profile`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/http-api/src/profile.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts).
> Package: `@vektorprogrammet/http-api@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Public HTTP contracts for the current user's profile.

Since v0.1.0

---

### Exports Grouped by Category

- [Endpoints](#endpoints)
  - [ReadOwnProfileEndpoint](#readownprofileendpoint)
  - [UpdateOwnProfileEndpoint](#updateownprofileendpoint)
- [Groups](#groups)
  - [ProfileApi (class)](#profileapi-class)
- [Schemas](#schemas)
  - [UpdateOwnProfileCommandExample](#updateownprofilecommandexample)
  - [UserProfileResponse](#userprofileresponse)
  - [UserRoleSchema](#userroleschema)

---

## Endpoints

### ReadOwnProfileEndpoint

Reads the current person's profile.

**Signature**

```ts
declare const ReadOwnProfileEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "readOwnProfile",
  "GET",
  "/api/me",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly personId: Schema.brand<Schema.String, "PersonId">;
      readonly firstName: Schema.String;
      readonly lastName: Schema.String;
      readonly email: Schema.String;
      readonly phone: Schema.String;
      readonly role: Schema.Literals<
        readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
      >;
      readonly nameRevision: Schema.Int;
      readonly contactRevision: Schema.Int;
    }>
  >,
  Schema.toCodecJson<
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["AuthorityInactive", "NotInScope"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfileNotFound", "ProfileContactNotFound"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<
            readonly ["ProfileStaleRevision", "ProfileCommandConflict"]
          >;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfileDecodeError"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfilePersistenceError"]>;
        }>;
      }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L108)

Since v0.1.0

### UpdateOwnProfileEndpoint

Updates the current person's profile with optimistic revisions.

**Signature**

```ts
declare const UpdateOwnProfileEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "updateOwnProfile",
  "PUT",
  "/api/me",
  never,
  never,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly _tag: Schema.Literals<readonly ["UpdateOwnProfile"]>;
      readonly commandId: Schema.brand<Schema.String, "ProfileCommandId">;
      readonly expectedNameRevision: Schema.Int;
      readonly expectedContactRevision: Schema.Int;
      readonly firstName: Schema.String;
      readonly lastName: Schema.String;
      readonly email: Schema.String;
      readonly phone: Schema.String;
    }>
  >,
  never,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly personId: Schema.brand<Schema.String, "PersonId">;
      readonly firstName: Schema.String;
      readonly lastName: Schema.String;
      readonly email: Schema.String;
      readonly phone: Schema.String;
      readonly role: Schema.Literals<
        readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
      >;
      readonly nameRevision: Schema.Int;
      readonly contactRevision: Schema.Int;
    }>
  >,
  Schema.toCodecJson<
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["AuthorityInactive", "NotInScope"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfileNotFound", "ProfileContactNotFound"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<
            readonly ["ProfileStaleRevision", "ProfileCommandConflict"]
          >;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfileDecodeError"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["ProfilePersistenceError"]>;
        }>;
      }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L126)

Since v0.1.0

## Groups

### ProfileApi (class)

Current-person profile endpoints.

**Signature**

```ts
declare class ProfileApi
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L149)

Since v0.1.0

## Schemas

### UpdateOwnProfileCommandExample

Representative self-profile update payload for generated examples.

**Signature**

```ts
declare const UpdateOwnProfileCommandExample: {
  readonly _tag: "UpdateOwnProfile";
  readonly commandId: string & Brand<"ProfileCommandId">;
  readonly expectedNameRevision: 0;
  readonly expectedContactRevision: 1;
  readonly firstName: "Ming";
  readonly lastName: "Medlem";
  readonly email: "ming.medlem@example.org";
  readonly phone: "+47 900 00 000";
};
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L62)

Since v0.1.0

### UserProfileResponse

Strict self-profile response exposed to the dashboard.

**Signature**

```ts
declare const UserProfileResponse: Schema.Struct<{
  readonly personId: Schema.brand<Schema.String, "PersonId">;
  readonly firstName: Schema.String;
  readonly lastName: Schema.String;
  readonly email: Schema.String;
  readonly phone: Schema.String;
  readonly role: Schema.Literals<readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]>;
  readonly nameRevision: Schema.Int;
  readonly contactRevision: Schema.Int;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L30)

Since v0.1.0

### UserRoleSchema

Legacy-compatible dashboard role projection.

**Signature**

```ts
declare const UserRoleSchema: Schema.Literals<
  readonly ["ROLE_ADMIN", "ROLE_TEAM_LEADER", "ROLE_TEAM_MEMBER"]
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/profile.ts#L18)

Since v0.1.0
