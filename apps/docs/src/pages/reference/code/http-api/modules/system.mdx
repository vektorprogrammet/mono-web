---
title: HTTP API / system
description: Generated code reference for packages/http-api/src/system.ts.
showAskAi: false
---

# HTTP API / `system`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/http-api/src/system.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts).
> Package: `@vektorprogrammet/http-api@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Public HTTP contracts for health and first-party session resources.

Since v0.1.0

---

### Exports Grouped by Category

- [utils](#utils)
  - [DeleteOwnedSessionEndpoint](#deleteownedsessionendpoint)
  - [DeleteSessionEndpoint](#deletesessionendpoint)
  - [HealthEndpoint](#healthendpoint)
  - [HealthOkResponse](#healthokresponse)
  - [HealthUnavailableResponse](#healthunavailableresponse)
  - [IdentityEngineUnavailableResponse](#identityengineunavailableresponse)
  - [ListSessionsEndpoint](#listsessionsendpoint)
  - [OwnedSessionNotFoundResponse](#ownedsessionnotfoundresponse)
  - [ReadSessionEndpoint](#readsessionendpoint)
  - [RevokeAllSessionsEndpoint](#revokeallsessionsendpoint)
  - [RevokeOtherSessionsEndpoint](#revokeothersessionsendpoint)
  - [SessionListResponse](#sessionlistresponse)
  - [SessionResponse](#sessionresponse)
  - [SystemApi (class)](#systemapi-class)

---

## utils

### DeleteOwnedSessionEndpoint

**Signature**

```ts
declare const DeleteOwnedSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "deleteOwnedSession",
  "DELETE",
  "/api/sessions/:sessionId",
  Schema.toCodecStringTree<Schema.Struct<{ sessionId: Schema.String }>>,
  never,
  never,
  never,
  Schema.toCodecJson<HttpApiSchema.NoContent>,
  Schema.toCodecJson<
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
        }>;
      }>
    | Schema.Struct<{
        readonly error: Schema.Struct<{
          readonly tag: Schema.Literals<readonly ["SessionNotFound"]>;
        }>;
      }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L103)

### DeleteSessionEndpoint

**Signature**

```ts
declare const DeleteSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "deleteSession",
  "DELETE",
  "/api/session",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<HttpApiSchema.NoContent>,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L83)

### HealthEndpoint

**Signature**

```ts
declare const HealthEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "health",
  "GET",
  "/health",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<Schema.Struct<{ readonly status: Schema.Literals<readonly ["ok"]> }>>,
  Schema.toCodecJson<Schema.Struct<{ readonly status: Schema.Literals<readonly ["unavailable"]> }>>,
  never,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L23)

### HealthOkResponse

**Signature**

```ts
declare const HealthOkResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["ok"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L12)

### HealthUnavailableResponse

**Signature**

```ts
declare const HealthUnavailableResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["unavailable"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L16)

### IdentityEngineUnavailableResponse

**Signature**

```ts
declare const IdentityEngineUnavailableResponse: Schema.Struct<{
  readonly error: Schema.Struct<{
    readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
  }>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L59)

### ListSessionsEndpoint

**Signature**

```ts
declare const ListSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "listSessions",
  "GET",
  "/api/sessions",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<
    Schema.$Array<
      Schema.Struct<{
        readonly sessionId: Schema.String;
        readonly createdAt: Schema.String;
        readonly updatedAt: Schema.String;
        readonly expiresAt: Schema.String;
        readonly ipAddress: Schema.NullOr<Schema.String>;
        readonly userAgent: Schema.NullOr<Schema.String>;
        readonly current: Schema.Boolean;
      }>
    >
  >,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L91)

### OwnedSessionNotFoundResponse

**Signature**

```ts
declare const OwnedSessionNotFoundResponse: Schema.Struct<{
  readonly error: Schema.Struct<{ readonly tag: Schema.Literals<readonly ["SessionNotFound"]> }>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L65)

### ReadSessionEndpoint

**Signature**

```ts
declare const ReadSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "readSession",
  "GET",
  "/api/session",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly sessionId: Schema.String;
      readonly createdAt: Schema.String;
      readonly updatedAt: Schema.String;
      readonly expiresAt: Schema.String;
      readonly ipAddress: Schema.NullOr<Schema.String>;
      readonly userAgent: Schema.NullOr<Schema.String>;
      readonly current: Schema.Boolean;
    }>
  >,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L71)

### RevokeAllSessionsEndpoint

**Signature**

```ts
declare const RevokeAllSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "revokeAllSessions",
  "POST",
  "/api/sessions::revoke-all",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<HttpApiSchema.NoContent>,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L132)

### RevokeOtherSessionsEndpoint

**Signature**

```ts
declare const RevokeOtherSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "revokeOtherSessions",
  "POST",
  "/api/sessions::revoke-others",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<HttpApiSchema.NoContent>,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L119)

### SessionListResponse

**Signature**

```ts
declare const SessionListResponse: Schema.$Array<
  Schema.Struct<{
    readonly sessionId: Schema.String;
    readonly createdAt: Schema.String;
    readonly updatedAt: Schema.String;
    readonly expiresAt: Schema.String;
    readonly ipAddress: Schema.NullOr<Schema.String>;
    readonly userAgent: Schema.NullOr<Schema.String>;
    readonly current: Schema.Boolean;
  }>
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L54)

### SessionResponse

Credential-free owner-only session metadata.

**Signature**

```ts
declare const SessionResponse: Schema.Struct<{
  readonly sessionId: Schema.String;
  readonly createdAt: Schema.String;
  readonly updatedAt: Schema.String;
  readonly expiresAt: Schema.String;
  readonly ipAddress: Schema.NullOr<Schema.String>;
  readonly userAgent: Schema.NullOr<Schema.String>;
  readonly current: Schema.Boolean;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L41)

### SystemApi (class)

**Signature**

```ts
declare class SystemApi
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L145)
