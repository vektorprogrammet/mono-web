---
title: HTTP API / system
description: Generated code reference for packages/http-api/src/system.ts.
showAskAi: false
---

# HTTP API / `system`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/http-api/src/system.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts).
> Package: `@vektorprogrammet/http-api@0.2.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Public HTTP contracts for health and first-party session resources.

Since v0.1.0

---

### Exports Grouped by Category

- [utils](#utils)
  - [DeleteOwnedSessionEndpoint](#deleteownedsessionendpoint)
  - [DeleteSessionEndpoint](#deletesessionendpoint)
  - [HealthEndpoint](#healthendpoint)
  - [HealthOkResponse](#healthokresponse)
  - [HealthUnavailableResponse](#healthunavailableresponse)
  - [ListSessionsEndpoint](#listsessionsendpoint)
  - [ReadSessionEndpoint](#readsessionendpoint)
  - [RevokeAllSessionsEndpoint](#revokeallsessionsendpoint)
  - [RevokeOtherSessionsEndpoint](#revokeothersessionsendpoint)
  - [SessionListResponse](#sessionlistresponse)
  - [SessionResponse](#sessionresponse)
  - [SystemApi (class)](#systemapi-class)

---

## utils

### DeleteOwnedSessionEndpoint

**Signature**

```ts
declare const DeleteOwnedSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "deleteOwnedSession",
  "DELETE",
  "/api/sessions/:sessionId",
  Schema.toCodecStringTree<Schema.Struct<{ sessionId: Schema.String }>>,
  never,
  never,
  Schema.toCodecStringTree<
    Schema.Struct<{ readonly "idempotency-key": Schema.brand<Schema.String, "IdempotencyKey"> }>
  >,
  Schema.toCodecJson<
    Schema.Codec<
      HttpApiSchema.withHeaders<
        void,
        { readonly "Cache-Control": "no-store"; readonly Vary: "Origin"; readonly ETag?: unknown }
      >,
      HttpApiSchema.withHeaders<void, Schema.StringTree>,
      never,
      never
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L139)

### DeleteSessionEndpoint

**Signature**

```ts
declare const DeleteSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "deleteSession",
  "DELETE",
  "/api/session",
  never,
  never,
  never,
  Schema.toCodecStringTree<
    Schema.Struct<{ readonly "idempotency-key": Schema.brand<Schema.String, "IdempotencyKey"> }>
  >,
  Schema.toCodecJson<
    Schema.Codec<
      HttpApiSchema.withHeaders<
        void,
        { readonly "Cache-Control": "no-store"; readonly Vary: "Origin"; readonly ETag?: unknown }
      >,
      HttpApiSchema.withHeaders<void, Schema.StringTree>,
      never,
      never
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L98)

### HealthEndpoint

**Signature**

```ts
declare const HealthEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "health",
  "GET",
  "/health",
  never,
  never,
  never,
  never,
  HttpApiSchema.WithHeaders<
    Schema.toCodecJson<Schema.Struct<{ readonly status: Schema.Literals<readonly ["ok"]> }>>,
    Schema.toCodecStringTree<
      Schema.Struct<{ "Cache-Control": Schema.Literal<"no-store">; Vary: Schema.Literal<"Origin"> }>
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  never,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L40)

### HealthOkResponse

**Signature**

```ts
declare const HealthOkResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["ok"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L29)

### HealthUnavailableResponse

**Signature**

```ts
declare const HealthUnavailableResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["unavailable"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L33)

### ListSessionsEndpoint

**Signature**

```ts
declare const ListSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "listSessions",
  "GET",
  "/api/sessions",
  never,
  never,
  never,
  never,
  HttpApiSchema.WithHeaders<
    Schema.toCodecJson<
      Schema.$Array<
        Schema.Struct<{
          readonly sessionId: Schema.String;
          readonly personId: Schema.brand<Schema.String, "PersonId">;
          readonly createdAt: Schema.String;
          readonly updatedAt: Schema.String;
          readonly expiresAt: Schema.String;
          readonly ipAddress: Schema.NullOr<Schema.String>;
          readonly userAgent: Schema.NullOr<Schema.String>;
          readonly current: Schema.Boolean;
        }>
      >
    >,
    Schema.toCodecStringTree<
      Schema.Struct<{
        "Cache-Control": Schema.Literal<"private, no-store">;
        Vary: Schema.Literal<"Origin">;
      }>
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L117)

### ReadSessionEndpoint

**Signature**

```ts
declare const ReadSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "readSession",
  "GET",
  "/api/session",
  never,
  never,
  never,
  never,
  HttpApiSchema.WithHeaders<
    Schema.toCodecJson<
      Schema.Struct<{
        readonly sessionId: Schema.String;
        readonly personId: Schema.brand<Schema.String, "PersonId">;
        readonly createdAt: Schema.String;
        readonly updatedAt: Schema.String;
        readonly expiresAt: Schema.String;
        readonly ipAddress: Schema.NullOr<Schema.String>;
        readonly userAgent: Schema.NullOr<Schema.String>;
        readonly current: Schema.Boolean;
      }>
    >,
    Schema.toCodecStringTree<
      Schema.Struct<{
        "Cache-Control": Schema.Literal<"private, no-store">;
        Vary: Schema.Literal<"Origin">;
      }>
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L77)

### RevokeAllSessionsEndpoint

**Signature**

```ts
declare const RevokeAllSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "revokeAllSessions",
  "POST",
  "/api/sessions::revoke-all",
  never,
  never,
  never,
  Schema.toCodecStringTree<
    Schema.Struct<{ readonly "idempotency-key": Schema.brand<Schema.String, "IdempotencyKey"> }>
  >,
  Schema.toCodecJson<
    Schema.Codec<
      HttpApiSchema.withHeaders<
        void,
        { readonly "Cache-Control": "no-store"; readonly Vary: "Origin"; readonly ETag?: unknown }
      >,
      HttpApiSchema.withHeaders<void, Schema.StringTree>,
      never,
      never
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L194)

### RevokeOtherSessionsEndpoint

**Signature**

```ts
declare const RevokeOtherSessionsEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "revokeOtherSessions",
  "POST",
  "/api/sessions::revoke-others",
  never,
  never,
  never,
  Schema.toCodecStringTree<
    Schema.Struct<{ readonly "idempotency-key": Schema.brand<Schema.String, "IdempotencyKey"> }>
  >,
  Schema.toCodecJson<
    Schema.Codec<
      HttpApiSchema.withHeaders<
        void,
        { readonly "Cache-Control": "no-store"; readonly Vary: "Origin"; readonly ETag?: unknown }
      >,
      HttpApiSchema.withHeaders<void, Schema.StringTree>,
      never,
      never
    >
  >,
  Schema.toCodecJson<Schema.Codec<unknown, unknown, never, never>>,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L168)

### SessionListResponse

**Signature**

```ts
declare const SessionListResponse: Schema.$Array<
  Schema.Struct<{
    readonly sessionId: Schema.String;
    readonly personId: Schema.brand<Schema.String, "PersonId">;
    readonly createdAt: Schema.String;
    readonly updatedAt: Schema.String;
    readonly expiresAt: Schema.String;
    readonly ipAddress: Schema.NullOr<Schema.String>;
    readonly userAgent: Schema.NullOr<Schema.String>;
    readonly current: Schema.Boolean;
  }>
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L72)

### SessionResponse

Credential-free owner-only session metadata.

**Signature**

```ts
declare const SessionResponse: Schema.Struct<{
  readonly sessionId: Schema.String;
  readonly personId: Schema.brand<Schema.String, "PersonId">;
  readonly createdAt: Schema.String;
  readonly updatedAt: Schema.String;
  readonly expiresAt: Schema.String;
  readonly ipAddress: Schema.NullOr<Schema.String>;
  readonly userAgent: Schema.NullOr<Schema.String>;
  readonly current: Schema.Boolean;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L58)

### SystemApi (class)

**Signature**

```ts
declare class SystemApi
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L220)
