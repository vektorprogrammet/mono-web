---
title: HTTP API / system
description: Generated code reference for packages/http-api/src/system.ts.
showAskAi: false
---

# HTTP API / `system`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/http-api/src/system.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts).
> Package: `@vektorprogrammet/http-api@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Public HTTP contracts for health and authenticated session reads.

Since v0.1.0

---

### Exports Grouped by Category

- [Endpoints](#endpoints)
  - [HealthEndpoint](#healthendpoint)
  - [ReadSessionEndpoint](#readsessionendpoint)
- [Groups](#groups)
  - [SystemApi (class)](#systemapi-class)
- [Schemas](#schemas)
  - [HealthOkResponse](#healthokresponse)
  - [HealthUnavailableResponse](#healthunavailableresponse)
  - [IdentityEngineUnavailableResponse](#identityengineunavailableresponse)
  - [SessionResponse](#sessionresponse)

---

## Endpoints

### HealthEndpoint

Database health endpoint.

**Signature**

```ts
declare const HealthEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "health",
  "GET",
  "/health",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<Schema.Struct<{ readonly status: Schema.Literals<readonly ["ok"]> }>>,
  Schema.toCodecJson<Schema.Struct<{ readonly status: Schema.Literals<readonly ["unavailable"]> }>>,
  never,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L44)

Since v0.1.0

### ReadSessionEndpoint

Strict current-session endpoint.

**Signature**

```ts
declare const ReadSessionEndpoint: HttpApiEndpoint.HttpApiEndpoint<
  "readSession",
  "GET",
  "/api/me/session",
  never,
  never,
  never,
  never,
  Schema.toCodecJson<
    Schema.Struct<{ readonly personId: Schema.String; readonly expiresAt: Schema.String }>
  >,
  Schema.toCodecJson<
    Schema.Struct<{
      readonly error: Schema.Struct<{
        readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
      }>;
    }>
  >,
  SessionSecurity,
  never
>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L84)

Since v0.1.0

## Groups

### SystemApi (class)

Operational and session endpoints.

**Signature**

```ts
declare class SystemApi
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L102)

Since v0.1.0

## Schemas

### HealthOkResponse

Healthy database observation.

**Signature**

```ts
declare const HealthOkResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["ok"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L16)

Since v0.1.0

### HealthUnavailableResponse

Unavailable database observation.

**Signature**

```ts
declare const HealthUnavailableResponse: Schema.Struct<{
  readonly status: Schema.Literals<readonly ["unavailable"]>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L30)

Since v0.1.0

### IdentityEngineUnavailableResponse

Identity provider failure response.

**Signature**

```ts
declare const IdentityEngineUnavailableResponse: Schema.Struct<{
  readonly error: Schema.Struct<{
    readonly tag: Schema.Literals<readonly ["IdentityEngineError"]>;
  }>;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L72)

Since v0.1.0

### SessionResponse

Authenticated session projection returned to browser applications.

**Signature**

```ts
declare const SessionResponse: Schema.Struct<{
  readonly personId: Schema.String;
  readonly expiresAt: Schema.String;
}>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/http-api/src/system.ts#L57)

Since v0.1.0
