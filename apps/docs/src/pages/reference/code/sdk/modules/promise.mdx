---
title: SDK / promise
description: Generated code reference for packages/sdk/src/promise.ts.
showAskAi: false
---

# SDK / `promise`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/sdk/src/promise.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/promise.ts).
> Package: `@vektorprogrammet/sdk@0.2.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Promise adapter for the Effect-native generated SDK.

Since v0.2.0

---

### Exports Grouped by Category

- [utils](#utils)
  - [ClientOptions](#clientoptions)
  - [PromiseSdk (type alias)](#promisesdk-type-alias)
  - [createConfiguredPromiseClient](#createconfiguredpromiseclient)
  - [createPromiseClient](#createpromiseclient)

---

## utils

### ClientOptions

**Signature**

```ts
declare const ClientOptions: ClientOptions;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/promise.ts#L51)

### PromiseSdk (type alias)

Converts an Effect client tree to the corresponding Promise client tree.

**Signature**

```ts
type PromiseSdk<A> = A extends (
  ...args: infer Args
) => Effect.Effect<infer Success, infer _Failure, infer _Requirements>
  ? (...args: Args) => Promise<Success>
  : A extends Readonly<Record<string, unknown>>
    ? { readonly [Key in keyof A]: PromiseSdk<A[Key]> }
    : A;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/promise.ts#L15)

### createConfiguredPromiseClient

Creates a Promise SDK from the SDK-owned environment configuration.

**Signature**

```ts
declare const createConfiguredPromiseClient: (options?: ClientOptions) => PromiseSdk;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/promise.ts#L48)

### createPromiseClient

Creates the complete Promise SDK projected from `ExternalNativeApi`.

**Signature**

```ts
declare const createPromiseClient: (
  baseUrl: string | undefined,
  options?: ClientOptions,
) => PromiseSdk;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/promise.ts#L42)
