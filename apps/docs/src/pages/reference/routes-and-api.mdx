---
title: Routes & API
description: Route families, capability owners, current availability, and native and legacy boundaries.
showAskAi: false
---

# Routes & API

[`ExternalNativeApi`](https://github.com/vektorprogrammet/mono-web/blob/c315db3b5809caffb16e149bd33f0a07b60948f6/packages/http-api/src/api.ts) is the public native HTTP contract. [`InternalNativeApi`](https://github.com/vektorprogrammet/mono-web/blob/c315db3b5809caffb16e149bd33f0a07b60948f6/packages/http-api/src/api.ts) is a separate internal contract.

## Current API availability

The native backend serves the route families below. Open the [generated native API reference](/reference/native-api) for all 52 public operations, schemas, and responses, or start with [Authentication & authorization](/reference/native-api/authentication) and [Getting started](/reference/native-api/getting-started).

The public document excludes Better Auth and all internal operations. The operation total does not claim legacy capability parity.

## Native route families

| Route family                                                                                                                                                                                   | HttpApi implementation       | Capability owner                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------- |
| `/api/auth/*`                                                                                                                                                                                  | Better Auth request handler  | Shared `AuthEngine` and `Identity` Layer        |
| `/api/session`, `/api/sessions*`                                                                                                                                                               | `SystemApiHandlers`          | `Identity`                                      |
| `/api/me`                                                                                                                                                                                      | `ProfileApiHandlers`         | `Profile`                                       |
| `/api/receipts*`, `/api/admin/receipts*`                                                                                                                                                       | `ReceiptApiHandlers`         | `Economy`                                       |
| `/api/applications*`, `/api/admission-periods/open`, `/api/admin/admission-periods*`                                                                                                           | `AdmissionsApiHandlers`      | `Admissions` and application domain programs    |
| `/api/admin/recruitment*`, `/api/recruitment/invitation-response*`                                                                                                                             | `RecruitmentApiHandlers`     | `Recruitment`                                   |
| `/api/departments`, `/api/teams`, `/api/field_of_studies`, `/api/admin/departments`, `/api/admin/teams`, `/api/admin/field-of-studies`, `/api/admin/team-interest`, `/api/admin/mailing-lists` | `OrganizationApiHandlers`    | `Organization`                                  |
| `GET /api/admin/schools`                                                                                                                                                                       | `AdminUsersApiHandlers`      | `Schools`                                       |
| `/api/admin/content*`                                                                                                                                                                          | `ContentApiHandlers`         | `ContentManagement`                             |
| `/api/news*`                                                                                                                                                                                   | `ContentApiHandlers`         | `Content`                                       |
| `GET /api/admin/users`                                                                                                                                                                         | `AdminUsersApiHandlers`      | Identity, profile, and organization projections |
| `GET /health`                                                                                                                                                                                  | `SystemApiHandlers`          | `Database`                                      |
| `GET /api/e2e/receipts/:receiptId/evidence` (internal ingress only)                                                                                                                            | `InternalReceiptApiHandlers` | `Economy` and the typed access evaluator        |

The contract groups are in [`packages/http-api/src`](https://github.com/vektorprogrammet/mono-web/tree/c315db3b5809caffb16e149bd33f0a07b60948f6/packages/http-api/src). The handler composition is in [`apps/backend/src/router.ts`](https://github.com/vektorprogrammet/mono-web/blob/c315db3b5809caffb16e149bd33f0a07b60948f6/apps/backend/src/router.ts).

## Boundary rules

1. `ExternalNativeApi` selects one public endpoint contract.
2. The matching `HttpApiBuilder` handler decodes transport input.
3. The handler resolves one actor and one authorization instant when the capability needs them.
4. The handler calls a Service program.
5. The handler maps typed outcomes to HTTP.
6. The handler does not select a live persistence implementation.

Unknown native paths return `404` with `RouteNotFound`.

`BACKEND_INGRESS=internal` constructs only `InternalNativeApi`. It does not mount public operations or Better Auth routes.

## Browser route boundary

React Router owns URL matching and server loaders. A Foldkit route bridge mounts a finite browser program.

Source areas:

- [`apps/dashboard/app/routes.ts`](https://github.com/vektorprogrammet/mono-web/blob/0bc4aa3ac5506b6371d25972c9608eee8e35c17d/apps/dashboard/app/routes.ts)
- [`apps/dashboard/app/routes`](https://github.com/vektorprogrammet/mono-web/tree/0bc4aa3ac5506b6371d25972c9608eee8e35c17d/apps/dashboard/app/routes)
- [`apps/dashboard/app/foldkit`](https://github.com/vektorprogrammet/mono-web/tree/0bc4aa3ac5506b6371d25972c9608eee8e35c17d/apps/dashboard/app/foldkit)

The route bridge can pass decoded input and forward messages. It must not duplicate the Foldkit `Model` or backend write state.

## Legacy API boundary

Legacy API Platform resources remain under [`apps/server/src/App`](https://github.com/vektorprogrammet/mono-web/tree/0bc4aa3ac5506b6371d25972c9608eee8e35c17d/apps/server/src/App).

Use the parity inventory to compare declared legacy and native routes. Do not treat similar path text as behavioral parity.
