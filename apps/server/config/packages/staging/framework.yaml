framework:
    rate_limiter:
        password_reset:
            policy: 'fixed_window'
            limit: 1000
            interval: '1 hour'
        contact_form:
            policy: 'fixed_window'
            limit: 1000
            interval: '1 hour'
        application:
            policy: 'fixed_window'
            limit: 1000
            interval: '1 hour'
