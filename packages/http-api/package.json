{
  "name": "@vektorprogrammet/http-api",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/vektorprogrammet/mono-web",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "bun run scripts/generate-openapi.ts",
    "generate:check": "bun run scripts/generate-openapi.ts --check",
    "check-types": "tsc --noEmit",
    "build": "bun run generate:check && tsc --noEmit",
    "lint": "oxlint",
    "test": "vitest run"
  },
  "dependencies": {
    "@vektorprogrammet/domain": "workspace:*",
    "effect": "catalog:effect-v4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "catalog:effect-v4"
  }
}
