---
title: Domain / content / content-service
description: Generated code reference for packages/domain/src/content/content-service.ts.
showAskAi: false
---

# Domain / `content / content-service`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/content/content-service.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/content-service.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [ArticleNotFound (interface)](#articlenotfound-interface)
  - [Content (class)](#content-class)
  - [ContentShape (interface)](#contentshape-interface)

---

## utils

### ArticleNotFound (interface)

Typed not-found carries no draft-existence information (law 5).

**Signature**

```ts
export interface ArticleNotFound {
  readonly _tag: "ArticleNotFound";
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/content-service.ts#L11)

### Content (class)

**Signature**

```ts
declare class Content
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/content-service.ts#L39)

### ContentShape (interface)

**Signature**

```ts
export interface ContentShape {
  /**
   * One complete listing snapshot, sticky-first then publishedAt DESC then
   * articleId DESC; pagination is a pure caller-side slice (law 10).
   */
  readonly readNewsListing: (
    departmentId?: DepartmentId,
  ) => Effect.Effect<
    PublishedNewsListing,
    ContentDecodeError | ContentDepartmentNotFound | ContentIntegrityError
  >;
  /**
   * Resolves one currently-published article by canonical slug with its
   * descending previous-version references.
   */
  readonly readPublishedArticle: (
    slug: string,
    versionNumber?: number,
  ) => Effect.Effect<
    PublishedNewsArticle,
    ContentDecodeError | ContentIntegrityError | ArticleNotFound
  >;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/content-service.ts#L15)
