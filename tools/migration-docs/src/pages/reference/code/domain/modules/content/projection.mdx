---
title: Domain / content / projection
description: Generated code reference for packages/domain/src/content/projection.ts.
showAskAi: false
---

# Domain / `content / projection`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/content/projection.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [NEWS_PAGE_SIZE](#news_page_size)
  - [dedupeSlug](#dedupeslug)
  - [filterNewsListingByDepartment](#filternewslistingbydepartment)
  - [filterWorkspaceByDepartment](#filterworkspacebydepartment)
  - [orderNewsSummaries](#ordernewssummaries)
  - [paginateNewsListing](#paginatenewslisting)
  - [projectContentWorkspace](#projectcontentworkspace)
  - [projectNewsSummaries](#projectnewssummaries)
  - [projectPublishedNewsArticle](#projectpublishednewsarticle)
  - [slugifyTitle](#slugifytitle)

---

## utils

### NEWS_PAGE_SIZE

Pure pagination slice over the fully loaded listing (page size 10).

**Signature**

```ts
declare const NEWS_PAGE_SIZE: 10;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L17)

### dedupeSlug

Deterministic deduplication against every existing slug: append `-2`,
`-3`, … until free. Pure over the taken-slug set.

**Signature**

```ts
declare const dedupeSlug: (base: string, takenSlugs: ReadonlySet<string>) => string;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L181)

### filterNewsListingByDepartment

Filters by department without disturbing the frozen ordering.

**Signature**

```ts
declare const filterNewsListingByDepartment: (
  listing: PublishedNewsListing,
  departmentId: DepartmentId | undefined,
) => PublishedNewsListing;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L127)

### filterWorkspaceByDepartment

Narrows the authorized set; it can never create authority.

**Signature**

```ts
declare const filterWorkspaceByDepartment: (
  workspace: ContentWorkspace,
  departmentId: DepartmentId | undefined,
) => ContentWorkspace;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L67)

### orderNewsSummaries

Sticky-first ordering; equal instants preserve the caller's article-id order.

**Signature**

```ts
declare const orderNewsSummaries: (
  summaries: ReadonlyArray<PublishedNewsSummary>,
) => PublishedNewsListing;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L106)

### paginateNewsListing

**Signature**

```ts
declare const paginateNewsListing: (
  listing: PublishedNewsListing,
  page: number,
) => PublishedNewsListing;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L119)

### projectContentWorkspace

Derives the staff workspace rows from raw draft facts plus the resolved
actor. `canRevise`/`canPublish` are pure projections of the actor matrix;
they never widen authority.

**Signature**

```ts
declare const projectContentWorkspace: (input: {
  readonly actor: ContentActor;
  readonly drafts: ReadonlyArray<{
    readonly articleId: ArticleId;
    readonly title: string;
    readonly slug: string;
    readonly sticky: boolean;
    readonly updatedAt: string;
    readonly currentVersionNumber: number | null;
    readonly departmentIds: ReadonlyArray<DepartmentId>;
    readonly createdByPersonId: string;
  }>;
  readonly authorDisplayNames: ReadonlyMap<string, string>;
}) => ContentWorkspace;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L29)

### projectNewsSummaries

**Signature**

```ts
declare const projectNewsSummaries: (input: {
  readonly versions: ReadonlyArray<{
    readonly slug: string;
    readonly title: string;
    readonly sticky: boolean;
    readonly publishedAt: string;
    readonly articleId: number;
    readonly authorPersonId: string;
  }>;
  readonly departmentsByVersionKey: ReadonlyMap<string, ReadonlyArray<DepartmentId>>;
  readonly authorDisplayNames: ReadonlyMap<string, string>;
}) => ReadonlyArray<PublishedNewsSummary>;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L77)

### projectPublishedNewsArticle

Builds the detail projection with descending previous-version references.

**Signature**

```ts
declare const projectPublishedNewsArticle: (
  summary: PublishedNewsSummary,
  bodyHtml: string,
  previousVersions: ReadonlyArray<{
    readonly versionNumber: number;
    readonly publishedAt: string;
    readonly slug: string;
  }>,
) => PublishedNewsArticle;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L141)

### slugifyTitle

Legacy `SlugMaker::setSlugFor`: lowercase, transliterate æøå to ae/o/a,
strip everything outside [a-z0-9-], collapse runs, trim hyphens.

**Signature**

```ts
declare const slugifyTitle: (title: string) => string;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/content/projection.ts#L167)
