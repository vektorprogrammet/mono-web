---
title: Domain / profile / service
description: Generated code reference for packages/domain/src/profile/service.ts.
showAskAi: false
---

# Domain / `profile / service`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/domain/src/profile/service.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts).
> Package: `@vektorprogrammet/domain@0.1.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

---

### Exports Grouped by Category

- [utils](#utils)
  - [DirectoryEntry (interface)](#directoryentry-interface)
  - [DirectoryPage (interface)](#directorypage-interface)
  - [Profile (class)](#profile-class)
  - [ProfileShape (interface)](#profileshape-interface)
  - [ReadDirectoryPageInput (interface)](#readdirectorypageinput-interface)
  - [UpdateOwnProfileInput (interface)](#updateownprofileinput-interface)

---

## utils

### DirectoryEntry (interface)

One directory entry: canonical names joined to canonical contacts.

**Signature**

```ts
export interface DirectoryEntry {
  readonly personId: PersonId;
  readonly firstName: string;
  readonly lastName: string;
  readonly email: string;
  readonly phone: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L55)

### DirectoryPage (interface)

**Signature**

```ts
export interface DirectoryPage {
  readonly entries: ReadonlyArray<DirectoryEntry>;
  /** Names the last emitted sort tuple; absent when the scan is exhausted. */
  readonly nextCursor?: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L63)

### Profile (class)

**Signature**

```ts
declare class Profile
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L69)

### ProfileShape (interface)

**Signature**

```ts
export interface ProfileShape {
  /** Reads a bounded set of canonical names and fails if any requested person is missing. */
  readonly readProfiles: (
    personIds: ReadonlyArray<PersonId>,
  ) => Effect.Effect<ReadonlyArray<PersonProfile>, ProfileFailure>;
  /** Reads canonical staff contacts and fails if any requested contact is missing. */
  readonly readContacts: (
    personIds: ReadonlyArray<PersonId>,
  ) => Effect.Effect<ReadonlyArray<PersonContactProfile>, ProfileFailure>;
  /** Reads the authenticated actor's canonical names and contact data. */
  readonly readOwnProfile: (personId: PersonId) => Effect.Effect<OwnProfile, ProfileFailure>;
  /**
   * Updates only the authenticated actor. The command deliberately has no target person field.
   */
  readonly updateOwnProfile: (
    input: UpdateOwnProfileInput,
  ) => Effect.Effect<OwnProfile, ProfileFailure>;
  /**
   * Scans the paged Profile directory: person_profiles joined to
   * person_contact_profiles ordered by lastName, firstName, then personId.
   * A missing contact row for a scanned person is a typed failure; the
   * read is read-only.
   */
  readonly readDirectoryPage: (
    input: ReadDirectoryPageInput,
  ) => Effect.Effect<DirectoryPage, ProfileFailure>;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L16)

### ReadDirectoryPageInput (interface)

**Signature**

```ts
export interface ReadDirectoryPageInput {
  /** Maximum number of persons on one page; strictly positive. */
  readonly limit: number;
  /**
   * Opaque continuation token naming the last emitted sort tuple
   * (lastName, firstName, personId). The next page resumes strictly after it.
   */
  readonly cursor?: string;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L44)

### UpdateOwnProfileInput (interface)

**Signature**

```ts
export interface UpdateOwnProfileInput {
  readonly actorPersonId: PersonId;
  readonly command: UpdateOwnProfileCommand;
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/domain/src/profile/service.ts#L11)
