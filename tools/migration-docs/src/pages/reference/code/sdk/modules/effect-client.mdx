---
title: SDK / effect-client
description: Generated code reference for packages/sdk/src/effect-client.ts.
showAskAi: false
---

# SDK / `effect-client`

> Generated by `@effect/docgen@4.0.0-rc.109` from [`packages/sdk/src/effect-client.ts`](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts).
> Package: `@vektorprogrammet/sdk@0.2.0`.
> The docs tool uses `typescript-docgen@6.0.2`.
> The repository TypeScript 7 checker remains separate.
> Edit the source comments, then run `bun run docs:generate`.

## Overview

Effect-native client projected directly from the canonical NativeApi contract.

Since v0.2.0

---

### Exports Grouped by Category

- [utils](#utils)
  - [ClientOptions (interface)](#clientoptions-interface)
  - [CookieOption (type alias)](#cookieoption-type-alias)
  - [EffectSdk (type alias)](#effectsdk-type-alias)
  - [FetchCapability (type alias)](#fetchcapability-type-alias)
  - [createConfiguredEffectClient](#createconfiguredeffectclient)
  - [createEffectClient](#createeffectclient)

---

## utils

### ClientOptions (interface)

Runtime options shared by every generated operation.

**Signature**

```ts
export interface ClientOptions {
  readonly cookie?: CookieOption;
  readonly fetch?: FetchCapability;
  readonly origin?: string;
  readonly headers?: Readonly<Record<string, string>> | (() => Readonly<Record<string, string>>);
}
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L19)

### CookieOption (type alias)

A Cookie header value or a lazy server-side Cookie header reader.

**Signature**

```ts
type CookieOption = string | (() => string | undefined);
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L13)

### EffectSdk (type alias)

Public Effect SDK shape generated by Effect from `ExternalNativeApi`.

**Signature**

```ts
type EffectSdk = NativeClient;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L97)

### FetchCapability (type alias)

Fetch capability supplied by the SDK composition root.

**Signature**

```ts
type FetchCapability = typeof globalThis.fetch;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L16)

### createConfiguredEffectClient

Creates an Effect SDK from the SDK-owned environment configuration.

**Signature**

```ts
declare const createConfiguredEffectClient: (options?: ClientOptions) => NativeClient;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L93)

### createEffectClient

Creates the complete Effect SDK directly from `ExternalNativeApi`.

The Effect HTTP API client owns request encoding, response decoding, and the
endpoint error channel. No SDK-local route registry or response decoder is
involved.

**Signature**

```ts
declare const createEffectClient: (
  baseUrl: string | undefined,
  options?: ClientOptions,
) => NativeClient;
```

[Source](https://github.com/vektorprogrammet/mono-web/blob/main/packages/sdk/src/effect-client.ts#L87)
