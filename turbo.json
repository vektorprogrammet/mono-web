{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["VEKTOR_POSTGRES_MAJOR"],
  "globalPassThroughEnv": ["VEKTORPROGRAMMET_HEAVY_LOCK"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "generate"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types", "generate"]
    },
    "api:spec": {
      "cache": false
    },
    "generate": {
      "dependsOn": ["^generate"],
      "outputs": ["openapi.json", "native-api-operations.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
